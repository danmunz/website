---js
const eleventyNavigation = {
	key: "Archive",
	order: 2
};
---
<h2>Archive</h2>
<article>
"<em>So...that's it. All of my darkest fears, and everything I'm capable of. That's me.</em>"<br/>—George Costanza

<h3>Categories</h3>

{% set allTags = [] %}
{% for post in collections.posts %}
  {% for tag in post.data.tags %}
    {% if allTags.indexOf(tag) == -1 and tag != "posts" and tag != "post" %}
      {% set allTags = allTags.concat([tag]) %}
    {% endif %}
  {% endfor %}
{% endfor %}



  {% for tag in allTags | sort %}
	  <a href="/tags/{{ tag | url }}" class="post-tag">{{ tag }}</a>
  {% endfor %}



<h3>All posts</h3>
{% set postslist = collections.posts %}
{% include "postslist.njk" %}
</article>